# OLED 显示与超声波测距

## 一、SSD1306 OLED 显示屏

### 1. 实验内容

点亮OLED屏

### 2. 实现要点

- 驱动文件是现成的（`hal_bsp_ssd1306.h/.c`，另有字库 `fonts` 和图片 `bmps` 头文件），我们只需要调用三个 API：
  - `SSD1306_Init()` —— OLED 初始化；
  - `SSD1306_CLS()` —— 清屏；
  - `SSD1306_ShowStr(x, y, str, size)` —— 在指定位置显示字符串。
- 时钟逻辑：创建线程 Task1，在 `while(1)` 里做**秒/分/时进位**（60 秒进 1 分、60 分进 1 时、24 时归零），每次先把显示缓冲区 `memset` 清零，再用 `sprintf` 按 `%02d:%02d:%02d` 格式化写入缓冲区，最后刷到 OLED 上，`sleep(1)` 实现一秒更新一次。


## 二、HC-SR04 超声波测距

### 1. 硬件连接与原理

- 模块通过 **GPIO7（Trig，输出触发）** 和 **GPIO8（Echo，输入回响）** 连接到 3861；
- 测距分四步：
  1. GPIO7 输出一个**至少 10µs 的高电平**触发信号（代码里给了 20µs）；
  2. 模块收到触发后自动发出超声波，同时让 Echo 引脚输出**高电平回响信号**；
  3. Echo 高电平的持续时间 = 超声波从发出到返回的往返时间；
  4. 距离公式：**distance = time × 0.034 / 2**（单位 cm）——声速约 340m/s，换算成 0.034cm/µs，除以 2 是因为声波走了"去 + 回"两趟。

### 2. 代码实现思路

核心是用**轮询方式捕获 Echo 高电平的持续时间**：

```c
while (1) {
    GpioGetInputVal(GPIO_8, &value);
    // 等到上升沿：记录起始时刻
    if (value == WIFI_IOT_GPIO_VALUE1 && flag == 0) {
        start_time = hi_get_us();
        flag = 1;
    }
    // 等到下降沿：计算高电平时长并退出
    if (value == WIFI_IOT_GPIO_VALUE0 && flag == 1) {
        time = hi_get_us() - start_time;
        break;
    }
}
distance = time * 0.034 / 2;
```

### 3. 两个必须注意的细节

- **先关看门狗**：任务入口处调用了 `WatchDogDisable()`。因为测距里的 `while(1)` 轮询可能长时间不退出，不喂狗的话系统会被看门狗复位；
- **测量周期**：线程里每 `osDelay(200)`（2 秒）测一次距并串口打印 `distance is %.1f (cm)`，不要疯狂连续测，给模块留出恢复时间。

---
